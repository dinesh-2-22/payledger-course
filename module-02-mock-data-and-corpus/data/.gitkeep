# This file keeps the empty data/ directory tracked in git.
# The generated CSVs themselves are gitignored (see repo-root .gitignore) -
# every learner regenerates them deterministically with generate_data.py.
